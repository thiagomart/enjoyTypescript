# enjoyTypescript

# Compilando arquivo TypeScript
- Compilando em tempo real:
tsc -w

# arquivo de configuração de TypeScript:
tsconfig.json
{
    "compilerOptions": {
        "outDir": "dist"
    }
}